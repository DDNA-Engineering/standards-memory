# Validation report

Date: 2026-09-22

Scope: `TASK-001` through `TASK-035` deterministic local evidence engine, authorization-safe installed-document discovery, stdio MCP adapter with model-facing MIL-STD reading guidance and operation-specific output schemas, official-source integrity boundary, PDF/derived-outline/reviewed-structure/corpus compilers, bounded reviewed semantic packets and procedure ordering, explicit lexical discovery modes, conservative edition-alignment candidates and side-qualified transitive dependency impacts, content-bound offline synthetic qualification, non-mutating portable readiness diagnosis, honest coverage, concise responses, exact page/structure spans, source-linked scoped discovery, executable response contracts and versioned generated-artifact schemas, dependency-complete CI, reproducible core-wheel acceptance and build provenance, a deterministic synthetic starter, a source-first engineering handoff, offline-verifiable release evidence, process-isolated PDF parsing, lossless storage/wire compression, policy-bound installation of the downloaded local MIL-STD corpus, acquisition-pinned prepared-distribution scope, and an offline model-ready prepared runtime

Repository state at validation: local uncommitted development working tree

## TASK-035 cross-platform release repair

Published `v0.1.0a4` proved the code-only PyPI trusted-publishing channel and the anonymous prepared ZIP download, but the independent native prepared-release run failed on Linux repeat setup and Windows extraction. On Linux, CPython's standard `venv.EnvBuilder(symlinks=False)` still creates `lib64 -> lib`; the second setup rejected that internal alias. The Windows acceptance workflow wrote a doubled carriage return into `GITHUB_ENV` and never reached setup. The public a4 release notes now disclose both limits; its successful publication is not treated as portable acceptance.

The a5 repair removes only the exact Linux `lib64 -> lib` alias after the prepared `.venv` ownership marker matches, then still rejects every other link in the virtual-environment tree. New environments receive the ownership marker before `EnvBuilder` runs, so an interrupted setup can be recovered without modifying an unowned environment. Linux negative tests confirmed that an external alias is rejected and an unowned environment's alias is untouched. The Windows workflow writes the extracted root to `GITHUB_ENV` as exact UTF-8 bytes with LF and now requires exact resolve plus replayed, source-verified `get_clause` evidence in addition to first/repeat setup, full-integrity doctor, and natural-language search.

Prepublication local validation passed all 170 Windows tests (two platform skips), nine focused Linux runtime tests, 49 requirements and 37 schema documents in contract validation, the eight-case synthetic benchmark, `actionlint`, and `git diff --check`. The reproducible a5 core wheel was built twice byte-identically at 155,248 bytes, SHA-256 `081b83021fa3642df1c09214857841ac73baac23164ffc7fb75180a09e3e2286`, and installed in an isolated environment outside the checkout. The a5 prepared ZIP is 1,058,683,424 bytes, SHA-256 `dd1def3e48c83c2e0ea54d27171177f8135065260591f8b60aac78bd089dc109`; it contains 438 DLA page-text packs, 35,218 page-text records representing 35,235 physical pages from 912 PDFs, and the separately labeled unreviewed MIL-STD-810H outline. Clean extracts completed first and repeat offline setup on native Windows under a path with spaces and non-ASCII text and on WSL Ubuntu's native filesystem; both repeated runs returned `already_ready`. Exact MIL-STD-810H(1) resolution and pinned source evidence retrieval succeeded through both Windows and Linux distribution launchers. These are local results, not yet a PyPI a5 publication, public a5 archive check, protected CI, or four-platform native release acceptance.

## StandardsForge rename verification

The project was renamed locally to StandardsForge on 2026-09-21. The distribution and Python module are `standardsforge`; the console commands are `standardsforge` and `standardsforge-mcp`. The README uses the new name and contains no emojis.

After the rename, editable installation succeeded, all existing tests passed, and contract validation returned the same two fixture digests recorded below. Both renamed console entry points returned help successfully, and both README demo blocks completed successfully. README file links and heading anchors were checked. The old editable distribution was removed from the local virtual environment.

Historical input names, build results, schema IDs, and pack provenance below retain their original identities. Existing evidence was not rewritten. New default local state is stored under `.standardsforge/`; `START_HERE.md` describes how to point the renamed commands at an existing store.

## Requirements addressed

The current `TASK-001` through `TASK-033` scope contains forty-seven bounded requirements. The following paragraph records the forty-five-requirement baseline through `TASK-031`; `TASK-032` adds offline prepared-release MCP readiness with a hash-inventoried dependency closure, distribution-rooted launcher, and pre-readiness stdio acceptance, and `TASK-033` adds authorization-safe installed-document discovery.

Forty-five bounded requirements are represented in `docs/requirements/requirements.json`, including identity/pinning, exact evidence, dependency context, pack contracts, all six read operations, exhaustive scoped traversal, edition comparison, rights-policy separation, offline operation, continuation binding, rebuildable indexes, completeness dimensions, official-source integrity, deterministic PDF compilation, extraction-fidelity separation, coverage propagation, source-spanned structure and procedure ordering, concise responses, batching, versioned authorization-safe caches, explicit lexical discovery modes, executable response-contract validation, prepared-distribution scope qualification, reproducible release provenance, lossless multi-span logical clauses, bounded reviewed semantic packets, content-bound synthetic qualification, non-mutating local readiness diagnosis, deterministic starter distribution, engineering handoff, release evidence, parser isolation, and explicit generated-artifact contract lifecycle. `TASK-007` adds a reviewed structural compiler slice, `TASK-008` adds the opt-in compact projection, `TASK-009` adds batched graph/source retrieval and scoped caches, `TASK-010` completes integrity verification and replayable exact-record selectors, `TASK-011` adds accurate concise surfacing, `TASK-012` removes duplicate portable page text and FTS content, preserves ambiguous relationship identity, reduces response finalization to one serialization, `TASK-013` adds manifest-driven, restartable compilation and policy-bound installation of the complete downloaded local MIL-STD corpus, `TASK-014` adds a separate deterministic unreviewed derived-outline representation with explicit unsupported regions, `TASK-015` adds exact one-pack policy onboarding plus client-visible MIL-STD reading instructions and tool boundaries, `TASK-016` removes superseded documentation and redundant comments while preserving active evidence and security guidance, `TASK-017` makes CI dependencies and machine response contracts executable, `TASK-018` binds prepared releases to the exact qualified acquisition snapshot, `TASK-019` binds two-build wheel reproducibility to final-bundle provenance, `TASK-020` adds lossless multi-span reviewed logical clauses, `TASK-021` adds a synthetic bounded reviewed semantic requirement packet with transitive governing context, `TASK-022` adds explicit phrase/all/any lexical discovery modes, `TASK-023` adds non-authoritative alignment candidates plus side-qualified transitive required-dependency impacts, `TASK-024` adds a synthetic-only offline qualification runner with executable artifact validation, `TASK-025` adds a portable doctor that opens state read-only and proves a policy-scoped exact query without migration or repair, `TASK-026` adds the deterministic synthetic starter, `TASK-027` adds a no-script engineering handoff with a static source-first reader, `TASK-028` adds deterministic SBOM and provenance sidecars plus a least-privilege main-push attestation job, `TASK-029` moves all untrusted PDF parsing behind a resource-limited disposable process boundary, `TASK-030` closes the generated-artifact schema and versioning gap, and `TASK-031` adds reviewed source-bound procedure-step ordering with required predecessor retrieval.

## Official-source seed corpus

The tracked catalogs record stable DLA Quick Search detail URLs and exact identities for three foundational format authorities plus MIL-STD-810H Change 1. Their PDF bytes are local ignored inputs under `.standardsforge/sources/dla/`; they are not repository content. The catalogs record publisher/source page counts, while local verification checks directory closure, regular-file properties, PDF signatures, byte lengths, and SHA-256 digests.

| Document | Edition | Bytes | Pages declared | SHA-256 |
|---|---|---:|---:|---|
| `MIL-STD-961` | Revision E, Change 5, 2025-11-25 | 7,272,070 | 127 | `92e0e2c1d39cb1b751d0c6807799ed651c81f20fb102cb3f26c3b392708876bd` |
| `MIL-STD-962` | Revision D, Change 3, 2025-05-06 | 6,099,517 | 85 | `663cf289b6c4ccc4773cd7709b674e6cc6aaa4d670b9db34bfdec96818fea2df` |
| `MIL-STD-967` | Change 3, 2025-08-27 | 4,964,679 | 67 | `f14dce9126a03f08a75a3c4273f42327c499dd074e7a518f9d00252f4a58d661` |
| `MIL-STD-810` | Revision H, Change 1, 2022-05-18 | 35,214,720 | 1,107 | `b34534a6e849b63b2c31c5a5d235b822e01849300b4ff9d4b18e692161f13935` |

This tracked seed set is not a project-applicability decision or the complete local military-standard acquisition. The larger acquisition manifest and its ignored local source bytes are compiled separately by `TASK-013`. The supplied product material explicitly names MIL-STD-961; MIL-STD-962 and MIL-STD-967 complete the format-authority set, while the user explicitly selected MIL-STD-810H for environmental-engineering coverage.

## Input provenance

The attached documents were treated as proposed product/build guidance and were not treated as evidence that a runtime already existed.

| Input | SHA-256 |
|---|---|
| `AGENTS.md` | `c634916cf29b1b90e0d04f2cf520483826bdffad6f0417c2bd3560c0f27ba140` |
| `START_HERE.md` | `bf87a035ff196943a7201800743945dca815b127f22790b54fa4d7d2f6160b38` |
| `Standards_Memory_PRD.docx` | `5a346b0660f4904422c7df13426c7e9f6b73c0397b2daae389a8028a47ae308f` |
| `Standards_Memory_Architecture_and_ADRs.docx` | `897b35823f1e3d0505b873f6f80b2e162295ef5b7e1aa749ccf3aa76acdd1aeb` |

## Commands and observed results

Current clean validation environment: Windows, Python 3.12.3, MCP Python SDK 2.2.0, pypdf 6.19.0, FontTools 4.65.0, and jsonschema 4.26.0. Isolated builds use the exact `setuptools==84.0.0` wheel pinned by filename, size, and SHA-256 in `build-toolchain.lock.json`.

```powershell
python -m pip install -e ".[mcp,compiler,contract]"
```

Observed: exit 0; the editable package and exact optional dependencies `mcp==2.2.0`, `pypdf==6.19.0`, `fonttools==4.65.0`, and `jsonschema==4.26.0` installed into an isolated local virtual environment. `python -m pip check` subsequently reported no broken requirements.

```powershell
$env:PYTHONPATH = Join-Path $PWD 'src'
python scripts/validate_contracts.py
```

Observed: exit 0. Twenty-one declared schemas passed their selected metaschemas; 13 concrete pack, policy, catalog, and benchmark-suite documents validated; 38 requirement identities resolved across `TASK-001` through `TASK-024`; nine detailed response packets, four compact/concise profile packets, three typed errors, and ten malformed negative response packets exercised the query contracts. Actual search packets cover all three explicit lexical modes and reject an undeclared mode. The actual response set includes a semantic record in detailed, compact, and concise profiles; false applicability, false human review, and semantics without review are rejected. Diff negatives reject incomplete moved evidence, missing changed-target status, status on an edge-delta cause, and a short dependency-edge tuple. Three positive semantic, review-event, and 0.2 annotation instances validated. Six negative instances rejected forbidden project applicability, false approval attestation, missing agent tool provenance, agent-claimed human review, 0.1 semantics, and a 0.2 agent annotation without a tool. Both first and final continuation pages were validated. The tracked benchmark suite also passed schema and executable review-digest validation.

```powershell
$env:PYTHONPATH = Join-Path $PWD 'src'
python -m unittest discover -s tests -v
```

Observed: exit 0; 97 tests passed in 16.027 seconds. In addition to the earlier install/read/tamper/coverage/cache cases, the suite preserves distinct ambiguous candidate sets and derivations, validates exact single-pass response budgets, migrates and rebuilds external-content FTS5, keeps FTS synchronized across insert/update/delete, reconstructs records 0.2 page text only from verified UTF-8 offsets, rejects PDF and sidecar tampering, proves deterministic archive identity, exercises both backward-compatible and `structured_only` MCP stdio modes, validates actual operation/profile/continuation/error packets, rejects malformed schemas and packets, verifies concrete MCP output schemas, exercises prepared-distribution scope and final-ZIP inventory checks, proves one bounded reviewed semantic packet with transitive note, definition, and table context, distinguishes exact-phrase, all-term, and any-term discovery through core, CLI, and MCP paths, and rejects forged benchmark responses, evidence text, digests, summaries, denominators, and omitted cases.

```powershell
$env:PYTHONPATH = Join-Path $PWD 'src'
python -m standardsforge verify-source-set catalog/mil-format-authorities.json --source-root .standardsforge/sources/dla/format-authorities
python -m standardsforge verify-source-set catalog/mil-std-810h.json --source-root .standardsforge/sources/dla/mil-std-810h
```

Observed: exit 0. Four local PDFs and 53,550,986 bytes were verified without network access. Every file matched its cataloged signature, byte count, and SHA-256 digest. Page counts were reported as declared metadata and were not recomputed by this command.

```powershell
python -m standardsforge compile-pdf catalog/mil-std-810h.json MIL-STD-810 `
  .standardsforge/compiled/mil-std-810h `
  --source-root .standardsforge/sources/dla/mil-std-810h
```

Observed: exit 0. The 1,107-page source produced 1,107 unreviewed page records, a 7,799,076-byte extracted-text sidecar, an 8,910,893-byte records document, and a 414,816-byte extraction report. The generated pack digest is `123e17f14c0e484eca5c03a4b34f3c5929f4946ff792711ee052b8fcd633b04c`. A second complete compile to a different directory produced the same digest. Rotated text was retained, with degraded layout and all visual fidelity still marked unverified.

The full pack validated, installed into an isolated local store in 3.218 seconds, resolved its exact edition, returned five ranked hits for `explosive atmosphere`, and retrieved the selected page with both raw-PDF and extracted-text hashes reverified. No OCR, model, or network fallback was used.

```powershell
python -m standardsforge compile-structure catalog/mil-std-810h.json MIL-STD-810 `
  .standardsforge/annotations/mil-std-810h-method-500.6-2.2.2.json `
  .standardsforge/compiled/mil-std-810h-method-500.6-2.2.2 `
  --source-root .standardsforge/sources/dla/mil-std-810h
```

Observed: exit 0. The explicitly agent-reviewed Method 500.6 section 2.2.2 slice on physical pages 86–87 produced six structural records and package digest `2babee8bf4085d2c3612939b4205613b83341dc0a97356b63943dbf18933dcbe`. Independent pack verification passed. An isolated authorized install and retrieval of item 2.2.2.d returned its governing NOTE as required evidence, retained the out-of-scope paragraph 1.3 reference, and reported `partial_reviewed_structural_section_only` with aggregate completeness false. This is not a document-wide semantic or human-review claim.

Canonical fixture measurements after compact-profile implementation were 3,342 detailed versus 3,412 compact bytes for one two-record clause packet, and 4,072 detailed versus 3,901 compact bytes for a three-record multi-clause context. The compact profile is therefore a measured reduction only for the latter fixture. No local tokenizer is configured; token counts are reported as unavailable rather than inferred from bytes.

`TASK-011` measured the exact MCP text JSON with the locally cached `tiktoken==0.9.0` `o200k_base` encoding; socket access was not needed. The two-record fixture measured 898 detailed, 905 compact, and 853 concise tokens. Three-record fixture context measured 1,121 / 1,060 / 999; one real page measured 1,042 / 1,106 / 1,015; eight pages measured 11,406 / 10,189 / 10,035; 64 pages measured 79,471 / 68,006 / 67,348; and reviewed structural evidence measured 2,456 / 2,370 / 1,779. These measurements name the encoding and representation; the product still reports token measurement unavailable because no runtime tokenizer is configured.

Post-`TASK-010` measurement on a disposable database copy of the installed 1,107-record MIL-STD-810H pack returned the same 511,658-byte 64-record detailed packet while reading the same two files and 43,013,796 bytes once per request. Ten warm runs measured p50 51.734 ms and p95 52.542 ms, compared with the pre-change audit's p50 766.055 ms and p95 812.378 ms. Fixture obligation enumeration measured p50 2.216 ms versus 4.936 ms before the change, with one 536-byte source read and five SQLite connections instead of two reads totaling 1,072 bytes and ten connections. These are local warm diagnostic samples, not production service-level guarantees.

A fresh `TASK-011` compile of all 1,107 MIL-STD-810H pages produced package digest `f4a5b84c1c9732ce7a073479c5dd4148e9ef1ba8dd548d7b3dc6f758ab662ae5`, explicit `page_text` representation metadata, and exact half-open sidecar offsets. Record IDs and exact text matched the earlier compile. Across 15 interleaved warm 64-page retrievals, first/middle/last groups measured p50 33.912 / 34.631 / 36.374 ms and p95 36.094 / 36.620 / 40.153 ms. The pre-fix audit measured p50 53.574 / 124.564 / 252.692 ms and p95 60.163 / 147.968 / 273.335 ms. Request-local peak Python allocation for the last 64 pages fell from the audit's approximately 90.5 MiB to 12.701 MiB by stream-hashing the preserved PDF while retaining the sidecar bytes needed for exact slices. The request still freshly hashes both preserved files; the change removes position-sensitive whole-sidecar quote searches and avoids retaining unused PDF bytes rather than weakening integrity checks.

Enriched real-pack search preserves the prior top-hit identities while adding FTS5 body snippets. After simplifying it to one FTS scan, 50 warm local SQL calls for `explosive atmosphere` returned 12 hits at p50 1.213 ms and p95 1.299 ms. Search and retrieval timings are diagnostic samples on this machine, not service-level guarantees.

`TASK-012` recompiled the complete 1,107-page source with offset-backed records schema 0.2. The uncompressed pack fell from 52,419,193 to 44,370,691 bytes, saving 8,048,502 bytes (15.35%); `records.json` fell from 8,988,094 to 939,593 bytes. Deterministic Deflate level 6 produced a 34,036,018-byte archive in 1.422 seconds and preserved package digest `469c7b5bb4dda187e5e913a0cf12d7a0d6087d5ceabfb913967514d9de479cb3` after extraction.

Installing that archive into a fresh schema-4 store produced a 12,603,392-byte database; the prior content-copy FTS database was 21,528,576 bytes. The authoritative `records` table remained 9,580,544 bytes and the FTS index data was 2,310,144 bytes, with no `records_fts_content` table. On 30 warm runs, one-page concise retrieval measured p50/p95 25.689/26.398 ms and a 64-page 481,283-byte concise packet measured 30.545/31.273 ms. The byte-identical finalizer fell from p50 6.723 to 2.391 ms; search measured p50/p95 1.656/1.844 ms across 50 runs. These remain machine-local warm-cache diagnostics.

Cross-package hard-link deduplication was not enabled. It would let mutation through any one package path alter every linked package. Safe transparent sharing requires filesystem-supported copy-on-write/block cloning or a contract-level package-path indirection and coordinated reference management; neither is silently assumed.

`TASK-013` verified the completed local DLA acquisition checkpoint at 464 active records, 967 selected current components, 912 downloaded PDFs / 1,113,274,833 source bytes, and 55 restricted metadata-only components. Manifest-driven compilation produced 438 deterministic record-scoped Deflate archives totaling 1,006,518,141 bytes. They preserve all 912 downloaded PDFs and expose 35,218 unclassified, unreviewed page records across 35,235 physical pages. The only missing text records are 17 malformed pages in MIL-STD-1661; their source bytes remain preserved and their extraction failures remain explicit. One empty-password public PDF was decrypted only for extraction while its original encrypted bytes remained the inventoried source. Compilation rejects a downloaded status unless its distribution statement is A and rejects a public component relabeled as restricted metadata.

An exact 438-pack local policy authorized installation for `local-user`. The default store contains all 438 current corpus package digests and active grants, with no index digest missing from the database or authorization set. The hardened reinstall revoked all 438 obsolete grants for the prior generated corpus version; the store retains those immutable historical objects rather than deleting them. The observed database therefore contains 877 packages and 70,439 records, while the active authorization set contains 439 grants: 438 current corpus packs plus one pre-existing three-record package. No obsolete corpus grant remains active, and all 35,218 current corpus records have statement role `unclassified`. Runtime acceptance resolved and retrieved MIL-STD-810H(1), the 129,439,361-byte MIL-STD-40051E(1) source, mixed public/restricted MIL-STD-167-2A NOT 1, and partially extracted MIL-STD-1661. Lexical search for `environmental engineering` returned source-linked MIL-STD-810H(1) evidence.

After `TASK-013`, contract validation passed for 13 contract documents, 30 requirements, and `TASK-001` through `TASK-013`. The complete local Python suite passed 67 tests in 8.579 seconds, covering networking-denied corpus composition, deterministic resume, corrupted and stale-valid archive rejection, distribution-status tampering, empty-password PDF handling, policy-bound batch install with obsolete-grant reconciliation, source retrieval, and transient Windows activation cases.

`TASK-014` compiled the current 1,107-record MIL-STD-810H page pack into a separately installable `derived_structure` pack with package digest `15546007f5f19963f3fc83cdd3da89484036348c49ff1bdf0c27f7bb42ca8f76`. The deterministic pass produced 7,788 exact-span records: 29 method headings, 240 sections, 1,627 clauses, 2,919 list items, 238 notes, 46 table captions, and 19 figure captions. It reported 188 wholly unsupported pages and 2,670 unsupported regions. Numeric table rows, repeated table-footnote runs, and repeated method headers are preserved as unsupported regions rather than allowed to alter ancestry. Every record remains `unclassified` and `automated_unreviewed`; obligation enumeration returned zero with aggregate completeness false.

The final pack installed under an exact local policy, resolved `MIL-STD-810H(1)` only when `derived_structure` was selected, and revoked the two superseded development grants. A pinned search for `low pressure` returned an exact source-linked table caption with only section 5.13 as ancestry. Exact retrieval of Method 500.6 clause 2.1 reverified the preserved PDF, extracted-text digest, physical page 85, UTF-8 bytes 636098:636643, and quote digest. Windows CLI output was additionally qualified against a CP1252-style console while emitting UTF-8 snippet markers.

After `TASK-014`, contract validation passed for 13 contract documents, 30 requirements, and `TASK-001` through `TASK-014`. The complete local Python suite passed 70 tests in 9.500 seconds, including networking-denied deterministic outline compilation, exact-span readback, table-row and footnote negative cases, non-page input rejection, sidecar tamper rejection, representation-specific resolution, exact evidence retrieval, and UTF-8 CLI output.

`TASK-015` added `write-pack-policy`, an explicit administrative command that validates one pack and requires the operator to name both the principal and expected content class before it writes an exact one-pack allowlist. Focused acceptance proved successful install and representation-specific resolution, rejection of a mismatched content class without creating a policy, rejection of an existing output, and the documented CLI argument shape. No query operation or MCP tool was added.

The MCP initialization result now carries the MIL-STD reading workflow, and all six client-visible tool descriptions carry operation-specific interpretation boundaries. Tests use a real MCP client to verify the initialization instructions, contract-description parity, unchanged tool names and schemas, read-only/closed-world annotations, startup-bound principal, and networking denial. The instructions distinguish exact editions and representations, immutable package pins, search candidates from retrieved evidence, governing context, tailoring from applicability, source requirements from verification methods, and zero classified obligations from proof of zero requirements.

The README's end-to-end Windows block covers isolated installation, corpus entry selection, outline compilation, explicit policy creation, installation, representation-specific resolution, pinned search, exact evidence retrieval, and MCP startup. PowerShell's parser accepted the complete block with no syntax errors, and CLI help exposed both onboarding commands. After `TASK-015`, contract validation passed for 13 contract documents, 30 requirements, and `TASK-001` through `TASK-015`; the complete local Python suite passed 73 tests in 8.445 seconds.

`TASK-016` removed three unreferenced dated audit snapshots whose implemented outcomes and measurements remain in this report, plus the duplicated `agents/BUILD_AGENT.md` workflow. `START_HERE.md` was reduced from 9,848 to 2,613 bytes while retaining the active baseline, required reading, validation commands, legacy-store migration, and rights/interpretation boundaries. Brand documentation now retains only usage and asset-format guidance. The decision index and security policy were updated from stale planned/fixture-only wording to the observed local public-source and MCP implementation without asserting owner approval.

Redundant MCP function docstrings, trivial class/helper docstrings, and a compatibility-restatement comment were removed. Comments explaining source integrity, relationship deduplication, unresolved-edge preservation, byte-budget arithmetic, transaction snapshots, and telemetry suppression remain because they document non-obvious correctness or security invariants. No TODO, FIXME, HACK, or XXX markers remain in source or tests.

After `TASK-016`, every retained repository Markdown link resolved locally, stale references to the removed audits and duplicated agent guide were absent, contract validation passed for 13 contract documents and `TASK-001` through `TASK-016`, compilation passed, `git diff --check` passed, and the complete local Python suite passed 73 tests in 8.966 seconds.

`TASK-017` corrected the CI profile from `.[mcp]` to `.[mcp,compiler,contract]`, added `pip check`, and retained zero core runtime dependencies. The executable contract gate checks every declared schema against its metaschema, validates concrete repository instances, and validates actual results from all six reads, both obligation-continuation states, compact and concise evidence profiles, typed domain/internal errors, and malformed negative cases. MCP tools now publish distinct self-contained success schemas instead of an unconstrained dictionary.

The installed-wheel gate copies only packaging inputs into a temporary source tree, builds the wheel twice under `SOURCE_DATE_EPOCH=1767225600` and `setuptools==84.0.0`, requires byte identity and fixed member timestamps, creates a fresh virtual environment, removes `PYTHONPATH` before forcing installation of the built wheel with no extras or dependencies, changes outside the checkout, and completes a core lexical-read smoke against a new local store under isolated Python. Its retained-output path emitted that exact wheel plus schema-validated provenance with the complete source-file inventory, builder versions, digest, size, epoch, and two-build result. Locally, dependency checking, contract validation, compilation, `git diff --check`, the then-current 80-test suite, and this isolated wheel gate all exited successfully. GitHub matrix execution remains unobserved until these local changes are committed and pushed.

`TASK-018` replaces the broad public-corpus wording with the recorded active MIL-STD, current-component, Distribution Statement A boundary. The prepared builder now requires the exact acquisition manifest digest pinned by the corpus, validates every acquisition record against the declared class and active-status filters, rejects discovery/acquisition/compilation failure and inconsistent count closure, validates an explicit source-baseline schema, preserves the exact acquisition manifest in the release, and reopens the completed ZIP to verify every inventory entry and the provenance digest link. First-run PowerShell setup repeats file and provenance checks before installing the wheel. A synthetic end-to-end distribution test observed the exact acquisition bytes and both provenance files inside the final ZIP, then proved an unlisted archive member is rejected.

`TASK-019` removes the seam between wheel acceptance and release assembly. The prepared builder now requires the retained wheel-provenance document, revalidates its schema, wheel identity, embedded package version, fixed timestamps, pinned generator, and every inventoried source-file hash, then inventories and digest-binds it in the outer manifest. The synthetic release fixture produced byte-identical final ZIPs from identical inputs and rejected a source changed after wheel verification. Parser process isolation remains a separate future security slice.

`TASK-020` extends versioned reviewed structure annotations from one source span to at most 64 ordered, non-overlapping exact spans while retaining the one-span 0.1 contract. The compiler reconstructs logical text only by joining those fragments with one newline, records every physical span, and inventories a separate exact assembled sidecar. Pack validation and each retrieval recheck all fragment offsets, text digests, quote digests, assembly, and the sidecar. Acceptance retrieved one logical clause spanning two synthetic PDF pages and rejected unordered spans plus post-install tampering of the second contributing page.

`TASK-021` adds optional reviewed semantics and content-bound review events to version 0.2 structural annotations and records. The closed semantic shape preserves subject, action, modality, polarity, conditions, exceptions, raw quantities, definitions, notes, table roles, unresolved issues, and the explicit `not_decided` project-applicability state. An agent review requires method and tool provenance and emits the exact attestation `extraction_review_not_project_applicability_or_approval`. Synthetic acceptance retrieved one multi-page obligation together with its required governing note and definition and the transitive table-footnote-to-row-to-header chain. Semantic span indices must be strictly source-ordered; detailed, compact, and concise profiles all preserve and schema-validate semantics and review. Invented or reordered semantic text, invalid semantic spans, kind mismatches, missing agent tool provenance, a changed reviewed-content digest, semantics without review, forbidden applicability, false human review, and a false approval attestation were rejected. This does not qualify the existing real reviewed structure pack, the prepared distribution, or any document-wide semantic coverage.

After `TASK-021`, contract validation passed for 19 schema documents, 35 requirements, and `TASK-001` through `TASK-021`; the complete suite passed 81 tests in 11.003 seconds. `pip check`, compilation, and `git diff --check` exited successfully. The isolated wheel gate built two byte-identical 101,342-byte wheels with SHA-256 `3cc3a9c2ac978114576578610d1c157db8239cecf74849d59d67b1c0ef2613e8`, installed the exact result outside the checkout without extras or dependencies, removed `PYTHONPATH`, and passed the core read smoke. Remote CI and a rebuilt prepared distribution remain unobserved.

`TASK-022` adds explicit `exact_phrase`, `all_terms`, and `any_terms` modes while preserving omitted-mode behavior as `all_terms`. The service parses at most 32 bounded lexical chunks, quotes each chunk, and compiles only the selected expression; familiar operators never become raw FTS syntax. Results expose the chosen mode, normalized query, and parsed chunks without changing source citations, snippets, ancestry, coverage, authorization, package/scope filters, or replayable evidence selectors. The mode participates in the version-2 cache key even when a one-chunk query produces identical FTS text. Core, CLI, MCP, and contract acceptance cover the three modes, default compatibility, invalid non-text/unknown modes, identifier-like chunks, and an undeclared returned mode. Document grouping, controlled terminology expansion, and semantic candidate retrieval remain future discovery slices.

After `TASK-022`, contract validation passed for 19 schema documents, 36 requirements, and `TASK-001` through `TASK-022`; the complete suite passed 83 tests in 11.351 seconds. `pip check`, compilation, and `git diff --check` exited successfully. The isolated wheel gate built two byte-identical 102,152-byte wheels with SHA-256 `3bb3fa77d39431adc3a78256d8f187f8933cbfd3334f7fe80613c50ae28dd5c6`, installed the exact result outside the checkout without extras or dependencies, removed `PYTHONPATH`, and passed the core read smoke. Remote CI and a rebuilt prepared distribution remain unobserved.

`TASK-023` keeps exact record IDs authoritative while reporting unique unmatched kind and clause-reference pairs only as content-hashed, review-required candidates. It adds `moved` for a source-location-only change and emits deterministic required-dependency paths separately for the before and after package graphs. Candidate identities never rewrite change statuses, edge deltas, or dependency paths, and the negative graph case proves that edges from different editions cannot fabricate a transitive path. Acceptance also proves that relocation plus substantive or derivation change remains `modified` and that an added required edge produces an `after`-side explanation path. Duplicate clause references are already rejected at pack validation, so ambiguous logical keys cannot enter the installed comparison domain. The 0.2 detailed diff contract and generated MCP schema close the packet, candidate, status-specific change, evidence derivation and structure, side-qualified path, cause, count, package, budget, and fixed four-item edge shapes. Reviewed cross-edition mappings, split/merge alignment, fuzzy equivalence, and semantic change classification remain future slices.

After `TASK-023`, contract validation passed for 19 schema documents, 37 requirements, and `TASK-001` through `TASK-023`; the complete suite passed 88 tests in 12.711 seconds. `pip check`, compilation, and `git diff --check` exited successfully. The isolated wheel gate built two byte-identical 105,417-byte wheels with SHA-256 `0742e64e0a00021defb12a9742824241ee33c468cdce00436815f0297913e4c9`, installed the exact result outside the checkout without extras or dependencies, removed `PYTHONPATH`, and passed the core read smoke. Remote CI and a rebuilt prepared distribution remain unobserved.

`TASK-024` adds closed synthetic benchmark-suite and benchmark-run contracts plus a tracked eight-case fictional suite covering exact resolution, all three lexical modes, both edition-specific context packets, outside-baseline honesty, and exact-identity revision comparison. The runner verifies the reviewed suite digest, exact pack identities and trusted local policy, denies networking during install and cases, preserves service-cache-cold and immediate-repeat-warm responses, validates every embedded query response, binds exact evidence text to citations, and recomputes case evaluations, response digests, summary arithmetic, release gates, and metrics before accepting an artifact. A rehashed wrong-edition response and rehashed altered evidence text are rejected. Discovery recall is 4/4 over discovery search cases only. Real-document discovery accuracy, semantic fidelity, tokenizer efficiency, peak resources, and engineer time remain explicitly not measured.

After `TASK-024`, contract validation passed for 21 schema documents, 13 concrete instances, 38 requirements, and `TASK-001` through `TASK-024`. The content-bound benchmark passed all eight cases with suite SHA-256 `c03782a8deeceb9c5b35eedc8fba9b2196768306a564c675cd539773e5afb4d8`; its local run artifact had digest `34e814aa08035df60c4b3e7622bce7fef63ee7b72da7cd5194231f2c25b087b3`. The complete suite passed 97 tests in 16.027 seconds; isolated `pip check`, compilation, and `git diff --check` exited successfully. The wheel gate built two byte-identical 114,653-byte wheels with SHA-256 `ddc1bd694bfc005660de66f14526785278dcf016cb048bb70444cc3077c27f40`, installed the exact result outside the checkout without extras or dependencies, removed `PYTHONPATH`, and passed the core read smoke. Remote CI and a rebuilt prepared distribution remain unobserved.

`TASK-025` adds a closed doctor report and CLI command over an explicit database, object store, trusted policy, and principal. It checks supported Python and SQLite FTS5, all required schema objects and metadata, read-only database integrity and foreign keys, installed package paths, policy-scoped active grants and fingerprints, quick selected-package or full installed-package integrity, optional or required MCP availability, and one deterministic `get_clause` smoke through the real read-only service. The read-only store seam uses SQLite `mode=ro` with `query_only`, bypasses directory creation and schema initialization, rejects installation and revocation, and still performs source verification plus final authorization. An absent target stays absent; the command never repairs, migrates, installs, revokes, fetches, or invokes a model. Exit `0` means ready, exit `3` is a completed not-ready report, and existing typed or internal errors remain exits `2` and `1`.

After `TASK-025`, contract validation passed for 22 schema documents, 13 tracked instances, 39 requirements, and `TASK-001` through `TASK-025`; it also validated one actual full-integrity doctor result and rejected an inconsistent readiness result. The focused doctor suite passed ten tests covering initialized and absent state, quick and full coverage, no-network and byte-stable diagnosis, source tamper, missing objects, path escape, unsupported schema without migration, policy drift, revocation, principal mismatch, optional or required MCP, CLI exit semantics, and mutation rejection. The complete suite passed 107 tests in 20.081 seconds. The content-bound synthetic benchmark passed all eight cases. Isolated `pip check`, compilation, and `git diff --check` exited successfully. The isolated wheel gate built two byte-identical 121,362-byte wheels with SHA-256 `aee9a4016ee4df1ddf3dbca652bfacb8398f6de0b947b7eee06d8012270d3010`, installed the exact core result outside the checkout without extras or `PYTHONPATH`, and passed both the existing read smoke and a full-integrity doctor exact-query smoke with MCP absent. Remote CI and a rebuilt prepared distribution remain unobserved.

`TASK-026` adds a separate deterministic synthetic starter product rather than weakening the prepared real-corpus distribution. Its closed manifest inventories the twice-built core wheel and exact source provenance, both fictional packs as raw data-only directories, the exact synthetic-only trusted policy, two starter contracts, license and claim boundary, and stdlib-only setup and launcher scripts. The builder validates current wheel source inventory, exact policy scope and content class, every pack, every payload path, and canonical stored-ZIP metadata; it builds twice, requires byte identity, reopens the archive, and rejects unsafe, duplicate, case-colliding, linked, extra, missing, or changed members. Setup validates every immutable extracted byte before writes, creates only marked local state, installs with indexes and dependency resolution disabled, validates and installs the exact packs, and writes the closed receipt only after full-integrity doctor, package-pinned discovery, and exact governing-context smokes. A rerun force-validates the wheel, bundle, receipt, packages, doctor, and evidence smokes instead of trusting a marker. Only a matching setup-owned partial state is recoverable.

After `TASK-026`, contract validation passed for 24 schema documents, 13 tracked instances, 40 requirements, and `TASK-001` through `TASK-026`. The focused starter suite passed ten tests, including wrong policy identity after reinventory, symbolic-link archive metadata, incomplete wheel provenance, output collision, duplicate/unlisted/unsafe members, tampering before writes, stale receipts, unowned state, setup-owned partial recovery, dependency-free pip arguments, and launcher exit forwarding. The complete suite passed 117 tests in 21.457 seconds; `pip check`, compilation, the eight-case content-bound benchmark, and `git diff --check` exited successfully. The isolated wheel gate built two byte-identical 122,188-byte wheels with SHA-256 `3ef760c805ab09e49309c067cf5504ea42649da23ed5334c7225a84c45f80bf8`, installed the exact core result outside the checkout without extras or `PYTHONPATH`, and passed both core smokes. The final starter is 200,497 bytes with SHA-256 `f0c6e7fa60e3989df18972d2fc0d220f33990ef2ce842c7833bf7eed57bdda20`. Acceptance extracted it under a path containing spaces with checkout `PYTHONPATH` removed, observed first setup `ready` and second setup `already_ready`, schema-validated the receipt, preserved immutable bundle bytes, passed doctor, discovery, and exact-context retrieval, and confirmed the MCP extra remained absent. The CI matrix now performs the same retained-wheel build and extracted-starter acceptance on Windows and Linux with Python 3.11 and 3.12; remote CI execution remains unobserved until these uncommitted changes are pushed. The large prepared distribution was not rebuilt.

`TASK-027` adds a closed, no-script engineering handoff export for one detailed evidence packet and caller-supplied candidate interpretation. The exporter reauthorizes before and after construction, preserves the authoritative packet unchanged, treats all candidate and agent material as untrusted draft content, inventories every file, activates the manifest last, and emits a static source-first reader with escaped text. Deterministic acceptance produced a handoff bundle with SHA-256 `4ca847bb9afb1e291496d6d4d0c2251859897ceb972a13c6e42d383a08bf1221` and manifest SHA-256 `d0061c8d7e3db4436f6e9c8cfb412ebff5583785a1a9e6737260af7002f96a9b`. Negative cases reject authorization drift, path and locator abuse, packet mismatch, unprovenanced agent content, injected decisions, tampering, links, extra files, and partial activation. This is a one-record static review surface, not a corpus-wide interactive reader.

`TASK-028` adds a closed five-file release-evidence directory: separate CycloneDX 1.7 SBOMs for the dependency-free core wheel and starter graph, separate unsigned in-toto Statement v1 documents using the SLSA provenance v1 predicate shape, and a canonical index that binds subjects, source inventory, dependency and build-tool locks, VCS state, and expected CI identity. The offline verifier reports integrity, source inventory, exact-commit binding, signature status, and authenticity separately and never fetches a URL. Local evidence is explicitly unsigned. The main-branch push job is separate from pull-request validation, receives identity-token and attestation permissions only at job scope after the test matrix succeeds, rebuilds the exact subjects, and invokes `actions/attest` at immutable commit `508db95dd578ae2727ebd6217d5ba78e4fbda05d`. Remote CI execution and a signed attestation bundle remain unobserved.

`TASK-029` moves every `pypdf` import and PDF object/text operation into one disposable worker per source. The parent creates the Windows Job Object before releasing the worker from its stdin gate; virtual environments start the base interpreter directly with site startup disabled and only the resolved package and dependency roots injected, preserving an active-process limit of one. POSIX workers install equivalent address-space, CPU, file-size, and core-dump limits before reading the request or source. The fixed policy limits a source to 256 MiB and 5,000 pages, page content and normalized page text to 64 MiB each, aggregate text to 256 MiB, result JSON to 4 MiB, process memory to 1 GiB, CPU to 300 seconds, and wall time to 600 seconds. The worker rechecks source identity and parser versions and activates canonical `result.json` last; the parent accepts only the exact flat regular-file set with matching ordinals, sizes, UTF-8 text, and digests. Parser failure is typed and forces incomplete corpus status rather than being relabelled as a no-text page.

Real Windows acceptance compiled the 35,214,720-byte, 1,107-page MIL-STD-810H Change 1 source through the isolated worker in approximately 154 seconds. All 1,107 page records were extracted, zero pages were relabelled no-text, the extraction report validated against its contract, and independent pack verification returned package digest `19718e36e0cf3a22566903cc38db82a6a34eddbf312d415353c99eb7b9340d79`. This observes one large source on Windows; Linux runtime behavior and broader worst-case resource suitability remain CI or future qualification boundaries.

Final validation after `TASK-029` passed `pip check`, 136 tests in 37.108 seconds in a clean virtual environment, compilation of `src`, `tests`, and `scripts`, workflow YAML parsing, `git diff --check`, all eight content-bound synthetic benchmark cases, 32 schema documents, 14 tracked instances, 43 requirements, `TASK-001` through `TASK-029`, one actual doctor result, one actual engineering handoff result, 12 negative query cases, and six negative semantic-contract cases. The exact locked backend built two byte-identical 140,924-byte wheels with SHA-256 `aaeed15b19bbbb62091cbfe43762b3b1b6acb5402f78a724936aa7ecdeadb811`; isolated installation outside the checkout passed dependency checking, core read, and full-integrity doctor smokes. A second fresh environment installed that exact wheel with only the pinned compiler dependencies and completed a real Job-Object-isolated PDF extraction, proving the worker bootstrap from installed wheel paths rather than the checkout. The final deterministic synthetic starter is 222,496 bytes with SHA-256 `09a5064824362f0da9040e2ab56b903a488c5fc818fb41ab9556c0716956776f`; extracted acceptance passed first and repeat setup, doctor, discovery, exact-context retrieval, path-with-spaces handling, receipt validation, immutable-byte checks, and absence of the MCP extra. The release-evidence index has SHA-256 `af1d69abe68be25252d682f154e4201bfee7fb6a01a1e59b8e98b3d5da7458e9`; offline verification proved subject and source-inventory integrity without network access. Because the working tree is intentionally uncommitted and dirty, its VCS binding is `context_only`; exact-commit and authenticity results are false, and signature status is `not_present`.

`TASK-030` verifies issue #15 against the current implementation rather than accepting its proposed fix verbatim. The existing gate already checked every schema against its declared metaschema, validated actual operation/profile/continuation/error packets, and rejected malformed schemas and packets. The remaining reproduced defect was narrower: the multi-component corpus compiler emitted a distinct extraction-report shape without a matching machine contract. A new closed corpus report schema now covers composition, compiler and parser-limit identity, aggregate counts, source components, encryption status, fidelity, and every page outcome; tests validate actual unencrypted and empty-password generated reports. The contract directory also defines an explicit versioning and append-only change-log policy that rejects unknown versions and forbids migrations from inventing semantic or approval state. Final local acceptance passed all 136 tests in 24.495 seconds, `pip check`, compilation, `git diff --check`, 33 schema documents, 14 tracked instances, 44 requirements, and `TASK-001` through `TASK-030`.

`TASK-031` verifies issue #7 against current behavior instead of treating its requested implementation as authoritative. Reviewed semantics already preserve actor, action, modality, polarity, conditions, exceptions, quantities, tailoring instructions, unresolved interpretation, and fixed undecided project applicability. The reproduced residual gap was ordered procedure context. Structure annotation 0.3.0 now accepts an exact source-spanned `sequence_after` edge only from one reviewed list-item step to one earlier sibling under the same explicit parent. The compiler and independent pack validator reject older-schema, optional, unresolved, duplicate, non-list, cross-parent, and backward predecessor claims; successful retrieval of the second step returns the first as required evidence while preserving undecided applicability. Local acceptance passed all 138 tests in 27.694 seconds and contract validation passed 33 schema documents, 14 tracked instances, 45 requirements, and `TASK-001` through `TASK-031`. This is reviewed source derivation, not a project-tailoring, test-adequacy, compliance, or approval decision.

`TASK-032` verifies issue #1 against the current prepared product. Generic source-checkout MCP guidance already existed, but the prior prepared archive contained only the dependency-free core wheel and could not load its own MCP entry point. Prepared-distribution 1.2 adds an exact hash-locked 29-wheel Windows x64 CPython 3.12 closure, independently recomputed requirements and wheelhouse digests, index-disabled installation with `pip check` plus an exact installed-environment check, isolated Python execution that ignores ambient `PYTHONPATH`, a distribution-rooted no-override launcher with absolute state paths and fixed principal, host configuration examples, and a pre-readiness stdio acceptance that requires exactly six read tools and an authorized search. A fresh isolated Windows acceptance installed the exact retained core wheel plus all 29 dependency wheels with package indexes disabled under a path containing spaces, rejected unlisted environment packages in its negative contract test, ignored deliberately poisoned ambient module paths, passed dependency checking, installed the fictional pack, and completed the stdio search from an unrelated working directory. The canonical wheelhouse inventory digest was `ba13d39dde766b291f8a08f231d0b80c0d983a16bb7e0c64fe7c18e24317dc13`; the exact requirements digest was `fa0547bc091073f39f0af977c99b4f9f965f9b748b033d2edff7bfb496dcf949`. Local validation passed all 141 tests in 32.851 seconds, 33 schema documents, 14 tracked instances, 46 requirements, and `TASK-001` through `TASK-032`.

The published MCP-ready `standardsforge-ready-0.1.0a1.zip` is 1,058,594,300 bytes with SHA-256 `df09a42b1a9d7f574666937f44048cb599dd0ccab53b182fdc59bd3c7ca5ac27`. Its 94,787-byte core wheel was built twice byte-identically from the tagged `v0.1.0a1` source and has SHA-256 `0e05285c99448a3a6c0bf1f2ffcea2761f89524d092cf669f506abea40811360`, preserving compatibility with the sealed corpus compiler 0.3 baseline instead of weakening the current compiler's fail-closed parser rules. A clean extraction under a path containing spaces and non-ASCII text completed first and repeat setup from an unrelated working directory, installed all 438 corpus packs plus the qualified outline, and wrote readiness only after its CLI and stdio checks. A separate client then launched the actual `standardsforge-mcp.ps1`, observed exactly `search`, `resolve_document`, `get_clause`, `build_context`, `enumerate_obligations`, and `diff_editions`, and received structured search output. GitHub reports the public ZIP at the same byte count and SHA-256; an anonymous checksum download returned that digest and an anonymous redirected request returned HTTP 200 with the same content length. Protected main-branch CI run `35735892280` and the separate push workflow run `35735892026` both passed for source commit `2df90876961aa4182cb46e78f6238fba7380a236` before publication.

`TASK-033` verifies issue #2 against current behavior rather than accepting every proposed remedy. Exact resolution did reproduce a generic `not_found` for the prefix `EXAMPLE-SPEC`, and neither the CLI nor MCP had a query-independent way to inventory installed documents. Search hits already contained `identifier`, `edition_id`, and `package_digest`, so that part was a description gap rather than missing result data. The implementation adds a seventh read-only `list_documents` operation with exact installed identity, representation, immutable package pin, record count, declared coverage, literal normalized-prefix filtering, stable signed pagination, and exact matching counts. Cursors bind operation, principal, prefix, visibility snapshot, position, count, and expiry. Resolution now returns at most twenty deterministic, replayable exact-selector, prefix, or matched-family candidates drawn only from current grants; it never adds fuzzy, title, network, model, or authorization fallback. Returned inventory rows and resolution results are reauthorized and visibility is rechecked before return. Query response contract 0.3 and the closed resolve near-match error contract validate actual first/final pages and failures, while the complete prior six-operation response contract remains available as 0.2. Local acceptance passed all 149 tests in 40.650 seconds, contract validation passed 35 schema documents, 14 tracked instances, 47 requirements, and `TASK-001` through `TASK-033`, dependency checking, compilation, and `git diff --check` passed, and the current seven-tool MCP smoke ran successfully against the actual extracted 438-pack prepared state. That real-state check returned both authorized `MIL-STD-810H(1)` representations for the prefix `MIL-STD-810` and returned the same two entries as bounded near-match candidates for `MIL-STD-810`. A forced concurrent revocation during near-match assembly failed with `authorization_changed`, and prepared readiness rejected empty authorized inventory and empty evidence search results.

The published maintenance release `v0.1.0a2` is pinned to source commit `246d729a8bdd344a44346e2cba2bb008a0109767`, a query-side backport onto the immutable `v0.1.0a1` baseline; it retains corpus compiler 0.3 and the same sealed 438-pack corpus rather than importing main's compiler 0.4 development state. Its 97,081-byte wheel was built twice byte-identically with SHA-256 `170b5c076e3b7e128e5563ecff1c0608add2c2ffa8f20ae5fcde8d76c7aaacbe`. The prepared archive is 1,058,596,821 bytes with SHA-256 `39d8b8260de74a1d0f95e3aba329b54e7646cdf2a9d53556689d4fb66258150d`; its closed manifest validated all 484 listed files with no missing, changed, or unlisted release files. A clean extraction under a path containing spaces and non-ASCII text completed first and repeat setup from an unrelated working directory, installed all 438 corpus packs plus the derived outline, traversed both signed inventory pages through the distribution CLI, returned the two typed near-match candidates, and launched the actual MCP wrapper with exactly seven tools. GitHub reports both assets uploaded with the expected size and digest, and a full anonymous download plus the separately downloaded checksum sidecar matched the same archive digest. Main CI run `35748443293` and CodeQL run `35748442255` passed implementation commit `b7014f0cfd4a4e9203bbaf00e57c7d035d059d36`. The maintenance tag commit has no GitHub check runs because the workflows do not run on that maintenance branch or tag; its evidence boundary is the isolated reproducible build and clean-extract acceptance recorded here. The snapshot still contains 35,218 page-text records representing 35,235 physical pages from 912 PDFs, so 17 physical pages remain without extracted text and this release does not improve corpus fidelity.

`TASK-034` verifies issue #3 against current behavior rather than treating the report as an implementation specification. The existing `exact_phrase`, `all_terms`, and `any_terms` modes, quoted FTS compilation, parsed-term bound, package/scope filters, and source-linked snippets were already present. The confirmed residual gap was natural-language lexical discovery: a prose question could still return no result, stemming was absent, MCP arguments lacked descriptions, and FTS5 BM25 statistics allowed unauthorized rows to alter visible scores. The opt-in `natural_language` mode now uses a separate external-content `porter unicode61` heading/text index, a fixed question-scaffolding policy that preserves modality, polarity, and conditions, strict stemmed AND followed by at most one disclosed stemmed OR attempt, and closed identifier-like prefix handling that does not broaden standalone quantities. Raw query characters and bytes, term count, and per-term characters and bytes are bounded before FTS execution. Every natural packet exposes parsed, effective, ignored, attempted, and selected interpretation plus tokenizer and length-normalized row-local ranker identities. Exact identifier selection remains the explicit `list_documents` and `resolve_document` path.

Schema 4-to-5 migration rebuilt both external-content indexes and their insert/update/delete triggers. A disposable copy of the published 438-pack `v0.1.0a2` database migrated from 317,747,200 to 362,336,256 bytes. Real-corpus prose queries for low-pressure testing, vibration testing, and temperature shock all selected the strict strategy and returned five authorized candidates; length-normalized cold searches completed locally in 0.024 to 0.080 seconds in the measured run. The low-pressure and temperature-shock top three included the qualified `MIL-STD-810H(1)` representation. These are observed lexical relevance samples, not a recall, applicability, or compliance claim. Focused tests additionally prove cache echo correctness, strict-to-relaxed disclosure, modality and condition preservation, scaffolding-only rejection without store access, numeric non-prefixing, schema-4 migration, trigger parity, revocation between strategies, unauthorized-row score isolation, typed MCP negative errors, closed mode-specific response shapes, and doctor rejection of a missing or non-Porter natural index.

Final local acceptance for `TASK-034` passed all 158 tests in 34.164 seconds, the eight-case synthetic benchmark, dependency checking, compilation, contract validation over 37 schema documents and 48 requirements, and `git diff --check`. Query response and success-envelope contracts are versioned 0.4 with retained 0.3 artifacts, and all seven MCP tools expose titles plus descriptions for every caller-visible argument.

The published maintenance release `v0.1.0a3` is pinned to source commit `0a85868c25d9fa48f3abc631cfded249781e2f88`, a query-side backport onto the immutable compiler-0.3 release line. Its 100,103-byte wheel was built twice byte-identically with SHA-256 `99bc6af31f2d0e7f303cac0b519dfad53a73d21d7557d8d719be59d1b4cffd72`. The prepared archive is 1,058,599,844 bytes with SHA-256 `6c989a8b03c2b5208dded606c99c3bc14b6ecdc136f2f7052da0ddc105cb58ac`; all 484 manifest entries matched exact lengths and digests with no extras or missing files. A clean extraction under a path containing spaces and non-ASCII text passed first and repeated setup, installed all 438 corpus packs plus the derived outline, returned five strict natural-language candidates through the real launcher, and returned five candidates through an actual stdio MCP client after verifying all seven tool titles, every input description, and the four-mode search schema. GitHub reports the public archive at the same byte count and digest, and a full anonymous download matched the separately downloaded checksum sidecar. Protected CI run `35758052000` and CodeQL run `35758050653` passed main implementation commit `8dcc0a7718e6f7705da4456dd3f2a5391503cc40`. The maintenance tag has no workflow run because those workflows do not run on maintenance branches or tags; its evidence boundary is the reproducible build, clean-extract acceptance, and public-download verification recorded here.

The superseded 1,043,153,557-byte prepared artifact described below predates `TASK-018` and did not contain the later acquisition, source-baseline, or MCP-runtime additions. Its earlier content/install acceptance remains historical evidence, not validation of the current artifact shape.

The prepared-distribution acceptance built `standardsforge-ready-0.1.0a1.zip` from the completed 438-pack corpus, the qualified MIL-STD-810H derived outline, an isolated StandardsForge wheel, exact public-content policies, and a file-level integrity manifest. The artifact is 1,043,153,557 bytes with SHA-256 `b95b4fd3623bf19cc02761612a16581b95acd621d361d6db9bcca4eb118502fa`. It contains 439 compiled packs representing 912 verified PDFs, 35,218 page records, and 35,235 physical pages. Restricted-only DLA records are not included.

Acceptance used a newly extracted directory with no existing database, object store, virtual environment, or `py` launcher. `setup.ps1` fell back to `python`, installed the bundled wheel without dependencies, validated and installed all 438 corpus packs, installed the derived outline, and completed its smoke query. The distribution launcher then resolved the exact `MIL-STD-810H(1)` `derived_structure` package and completed a separate `low pressure` search. No source acquisition, PDF compilation, model call, or network access occurred during corpus setup or query. This historical artifact was published and was replaced in place by the `TASK-032` artifact recorded above.

After the distribution change, contract validation and `git diff --check` passed. The complete suite passed 73 tests in 9.333 seconds under the repository `.venv` with pinned `pypdf==6.19.0` and MCP SDK 2.2.0. A run under the unrelated system Anaconda environment was rejected by the compiler dependency guard and incompatible MCP import, confirming that repository validation must use the documented isolated environment.

The first FontTools-free corpus attempt was stopped after pypdf exposed incomplete CFF Type1 decoding. Its 340,857,086 bytes of generated output were moved intact to ignored quarantine at `.standardsforge/generated-artifact-quarantine/mil-std-current-pre-fonttools`; source files were not changed. The pre-hardening 438-pack corpus and policy were likewise moved intact to ignored quarantine before regeneration. The final compiler pins `pypdf==6.19.0` and `fonttools==4.65.0`, records both versions in derivations, and was rebuilt from a fresh output root. Transient OneDrive `WinError 5` conditions during compiled-directory and object-directory activation were reproduced with focused tests and handled through bounded retry followed by the same package and archive validation.

```powershell
$env:PYTHONPATH = Join-Path $PWD 'src'
python -m compileall -q src tests scripts
```

Observed: exit 0.

```powershell
python -m pip wheel . --no-deps --wheel-dir <ignored-local-directory>
```

Observed: exit 0; `standardsforge-0.1.0a1-py3-none-any.whl` built successfully with isolated build dependencies and contained the compiler, source-catalog verifier, MCP server module, and both console entry points. A stale pre-rename build directory initially caused obsolete `standards_memory` modules to leak into a wheel; those generated artifacts were quarantined under ignored local state, the wheel was rebuilt from a clean build directory, and the final wheel contains no `standards_memory` package. The artifact remains in ignored local validation state.

The pre-publication credential-pattern and absolute-local-path scans passed with zero matches.

## Fixture digests

| Pack | Edition | Package digest |
|---|---|---|
| `example.vehicle-adapter.1.0` | `example:spec-100:2025-a` | `3c27ee682401e154425f806614a800d744f472f1be41e9e412f9214460f12117` |
| `example.vehicle-adapter.2.0` | `example:spec-100:2026-b` | `ad93262351fbf1138f841e7f964fca95ffe7c5139778996ec851bdd91df33ad3` |

## Security and rights effects

Pack rights records are preserved as claims and cannot authorize installation or serving. An external operator policy must authorize the exact pack ID and content class; its fingerprint is stored with a digest-bound principal grant. Reads check active authorization before access and again before returning an evidence packet. Query operations do not import, activate, publish, execute, download, invoke models, or create sockets.

The MCP adapter accepts its principal only from trusted process startup configuration and exposes no administrative tool. It runs only over stdio, creates no listener, and removes the pinned SDK's OpenTelemetry middleware before accepting calls. Expected domain failures are marked as MCP errors and carry the existing typed JSON error envelope without returning the bound principal.

Data-only suffix restrictions, inventory closure, byte counts, hashes, source quote checks, dependency resolution, path traversal checks, symlink denial, and bounded archive size/file counts are enforced during pack validation. Source-catalog validation independently enforces a closed directory, safe single-component PDF filenames, stable official-source URLs, PDF signatures, byte counts, digests, and non-assertion of repository redistribution rights.

The single-PDF compiler rejects encrypted PDFs; the DLA corpus compiler additionally accepts empty-password public encryption and rejects non-empty passwords. All PDF parsing occurs in a disposable resource-limited child process, never in the long-lived compiler parent. Both paths reject mismatched declared page counts, oversized source/page streams, malformed worker output, source changes, missing document-wide text layers, and conflicting outputs. Raw PDF sources and extracted text are independently inventoried and rechecked. PDF actions are never executed. Rights metadata remains provenance and cannot grant install or query permission.

## Migration and rollback

The local database is schema version 5. Tested v1/v2/v3-to-v5 migrations add explicit derivation/statement-role/structure fields where needed, and the tested v4-to-v5 migration rebuilds the exact and natural-language FTS5 external-content projections and their triggers. Legacy derivations remain `unknown`/`unreviewed`; migrations never invent obligation classifications or structure. Installation is content-addressed and metadata activation is transactional. To roll back a development instance, stop using the local CLI and remove only its explicitly selected database/object directory. Grant revocation provides immediate logical denial without deleting evidence bytes.

## Limitations

- Real PDF text layers are compiled and source-linked. One reviewed structural section and one synthetic reviewed semantic packet are qualified, but document-wide visual reading order, tables, figures, OCR, semantic clauses, dependencies, and obligation classification are not qualified.
- The seven read operations are implemented in the local library, CLI, and stdio MCP adapter; no HTTP transport is implemented, and a hosted multi-tenant service is not a product target.
- A one-record static source-first reader and deterministic CycloneDX release SBOMs are implemented. There is no corpus-wide interactive reader, persistent background compilation service, model adapter, broad performance benchmark, configured runtime tokenizer, or locally observed signed release artifact.
- Exact quote presence is not proof of PDF fidelity, and returned evidence is not an applicability, compliance, or human approval decision.
