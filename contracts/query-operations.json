{
  "contract_version": "0.2.0",
  "read_operations": [
    {"name": "search", "requires_principal": true, "requires_package_pin": false, "optional_arguments": {"package_digest": "authorized immutable package pin", "scope_prefix": "exact clause reference or dot-delimited descendants"}, "query_limits": {"maximum_parsed_terms": 32, "overflow_error": "invalid_query", "all_parsed_terms_are_required": true}, "packet_fields": {"snippet_markers": {"start": "⟦", "end": "⟧"}}, "result_fields": {"identifier": "installed document identifier", "edition_id": "installed edition identity", "package_digest": "immutable package pin", "source": "exact installed record citation", "matched_snippet": "FTS5 snippet from the indexed record text", "heading_ancestry": "optional structural parent records in root-to-parent order; omitted when absent"}},
    {"name": "list_documents", "requires_principal": true, "requires_package_pin": false, "optional_arguments": {"identifier_prefix": "normalized literal identifier prefix", "limit": "page size from 1 to 100", "cursor": "signed principal, prefix, visibility, and position-bound continuation"}},
    {"name": "resolve_document", "requires_principal": true, "requires_package_pin": false, "optional_arguments": {"edition_id": "exact edition identity", "representation": ["page_text", "derived_structure", "reviewed_structure", "curated_records"]}},
    {"name": "get_clause", "requires_principal": true, "requires_package_pin": true, "accepted_record_kinds": ["clause", "note", "page"], "selector": {"at_least_one": ["clause_reference", "record_id"], "when_both": "must_resolve_to_same_record"}, "optional_arguments": {"clause_reference": "unique record reference within the pinned package", "record_id": "canonical record identity within the pinned package", "response_profile": ["compact_evidence_v1", "concise_evidence_v1"]}},
    {"name": "build_context", "requires_principal": true, "requires_package_pin": true, "optional_arguments": {"response_profile": ["compact_evidence_v1", "concise_evidence_v1"]}},
    {"name": "enumerate_obligations", "requires_principal": true, "requires_package_pin": true},
    {"name": "diff_editions", "requires_principal": true, "requires_package_pin": true}
  ],
  "admin_operations": [
    {"name": "verify_pack", "authorization_effect": "none"},
    {"name": "install", "requires_operator_policy": true},
    {"name": "revoke", "requires_existing_grant": true}
  ],
  "local_mcp_adapter": {
    "transport": "stdio",
    "principal_binding": "trusted_server_startup_configuration",
    "principal_argument_exposed": false,
    "admin_operations_exposed": false,
    "telemetry": "disabled"
  },
  "response_profiles": {
    "default": "detailed_json_v1",
    "supported": ["detailed_json_v1", "compact_evidence_v1", "concise_evidence_v1"],
    "compact_response_schema": "compact-evidence-response.schema.json",
    "concise_response_schema": "concise-evidence-response.schema.json",
    "token_measurement": "unavailable_without_a_configured_local_tokenizer",
    "byte_measurement": "compact_utf8_json_packet_including_budget_metadata"
  },
  "invariants": [
    "query operations do not fetch or activate content",
    "document inventory and resolve near-match candidates contain only packages actively authorized for the bound principal",
    "document inventory continuations bind operation, normalized prefix, principal, visibility snapshot, position, count, and expiry",
    "resolve_document reports representation on success and in ambiguity candidates; exact package pins remain authoritative",
    "get_clause selectors are directly replayable from search results and cannot silently resolve a mismatched clause-reference and record-ID pair",
    "package rights claims do not grant operational access",
    "required evidence is atomic under byte budgets",
    "continuations bind package, scope, principal, policy, and expiry",
    "edition diffs do not infer moves from similarity",
    "compact and concise evidence profiles preserve exact text and canonical record IDs while deduplicating packet-local source and derivation dictionaries",
    "omitting response_profile preserves the existing detailed JSON representation",
    "search filters are exact local constraints and never relax or invoke network or model fallback",
    "search quotes and requires every parsed term up to the declared limit; overflow fails with invalid_query rather than dropping terms",
    "search returns a source citation, an FTS5 snippet from the exact indexed record text, and structural heading ancestry when parent records exist",
    "search remains lexical and never silently combines exact document resolution with record search"
  ]
}
